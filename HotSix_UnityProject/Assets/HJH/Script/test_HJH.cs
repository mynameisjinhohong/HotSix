using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class test_HJH : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //지금 나의 주석이 읽힌다면 읽힌다고 말하셈.
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
